{% extends 'HBBlogBundle::layout.html.twig' %}

{% block content -%}
    <h1>Article edit</h1>

    {# <form action="{{ path('article_update', {"id": entity.id}) }}" method="put" {{ form_enctype(edit_form) }}>
        {{ form_widget(edit_form) }}
    </form> #}
    
    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('article') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
