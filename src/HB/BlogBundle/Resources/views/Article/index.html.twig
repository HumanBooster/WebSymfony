{% extends 'HBBlogBundle::layout.html.twig' %}

{% block content -%}
    <h2>Liste des articles</h2>


    {% for article in articles %}
        {% if loop.first %}<ul>{% endif %}
            <li><a href="{{ path('article_show',{'id':article.id}) }}">{{ article.title }}</a>
- <a href="{{ path('article_edit',{'id':article.id}) }}">éditer</a>
- <a href="{{ path('article_delete',{'id':article.id}) }}">supprimer</a></li>
            {% if loop.last %}</ul>{% endif %}
        {% else %}
        <p>Aucun article.</p>
    {% endfor %}

    <p><a href="{{ path('article_new') }}">Ajouter un article</a></p>
{% endblock %}
